name: Flutter Capado Build

on:
  workflow_dispatch:

jobs:
  run-flutter-capado-build:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "capado"